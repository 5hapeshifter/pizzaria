server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pizzaria
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect